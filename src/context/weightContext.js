import { createContext } from "react";

/**
 * Description: Created a weigh context for the Test Volume/Weigh of water(for Data Tables) to directly
 * pass its value in other components using context api and to avoid prop drilling
 *
 * @exports The Weigh Context
 */
export const weighContext = createContext();
